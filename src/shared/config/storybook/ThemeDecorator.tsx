import { Decorator } from '@storybook/react';
import { Theme } from 'app/providers/ThemeProvider';
import '../../../app/styles/index.scss';

// export const ThemeDecorator: Decorator = (theme: Theme) => (
//   <div className={`app ${theme}`}></div>
// );
